info:
  name: GET_file
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:8000/core/api/files/objects/{{OBJ_ID}}/files/{{BLOB_ID}}
  headers:
    - name: Authorization
      value: Bearer {{AUTH_TOKEN}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
