info:
  name: GET_metadata
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:8000/core/api/files/objects/{{OBJ_ID}}
  headers:
    - name: Authorization
      value: Bearer {{AUTH_TOKEN}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
